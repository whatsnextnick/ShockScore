# Production Environment Variables for Netlify

# Disable source maps for production (smaller build size)
GENERATE_SOURCEMAP=false

# API URL (empty means use same domain with redirects)
REACT_APP_API_URL=

# Build optimizations
INLINE_RUNTIME_CHUNK=false

# Disable treating warnings as errors in CI
CI=false
